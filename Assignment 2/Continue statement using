def print_divisible_by_3(numbers):
    for num in numbers:
        if num % 3 != 0:
            continue  # Skip numbers not divisible by 3
        print(num)

# Example usage
numbers = [1, 3, 4, 6, 9, 10, 12, 14, 15]
print_divisible_by_3(numbers)
